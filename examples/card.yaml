# Compact room card. Use the entity from the room-creation notification or the
# room status sensor's `card_yaml` attribute.
type: custom:smart-shading-card
entity: sensor.replace_with_generated_room_status
show_sun_track: true
show_covers: true
show_actions: true
